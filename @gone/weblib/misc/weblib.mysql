
# MySQL code for weblib session table..
# $Id$

CREATE TABLE web_sess (
  sid varchar(32) DEFAULT '' NOT NULL,
  name varchar(32) DEFAULT '' NOT NULL,
  sess text,
  tsUpdate datetime,
  primary key (sid, name),
);

