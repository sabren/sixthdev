"""
This is just a dummy file I was using
so I didn't have to run py2exe every time
I made a change to cvssh.py.

(I compiled this with build.bat and set CVS_RSH=stub)
"""
import os
import popen2
import socket, select
exec open("cvssh.py")
